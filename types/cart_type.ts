export type Book = {
  title: string;
  description: string;
  imageUrl: string;
};

export type Review = {
  id: string;
  author: string;
  text: string;
};

export type Post = {
  id: string;
  text: string;
};
